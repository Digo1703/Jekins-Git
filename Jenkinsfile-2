pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo "Build number is ${currentBuild.number}"
                echo "Build name is ${build_display_name.name"
            }
        }
    }
}
